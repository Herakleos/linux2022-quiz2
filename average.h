#ifndef AVERAGE_H
#define AVERAGE_H

#include <stdio.h>
#include <stdint.h>

// EXP1
uint32_t average(uint32_t a, uint32_t b);

// EXP2, EXP3
uint32_t average2(uint32_t a, uint32_t b);

#endif  // AVERAGE_H